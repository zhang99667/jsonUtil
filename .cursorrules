# JSONUtils 专业版 - Cursor AI 规则

## 📖 完整规范文档

**详细的编码规范请参考**: `rules/code-style.md`

本文件提供快速参考和 Cursor AI 特定的配置。

---

## 项目上下文

JSON 处理工具，包含前后端分离架构和 Electron 桌面应用。

### 技术栈
- **前端**: React 19 + TypeScript + Vite 6 + Tailwind CSS + Monaco Editor
- **后端**: Spring Boot 3.x + Java 17 + MySQL
- **桌面**: Electron
- **AI**: Google Gemini API

### 关键文档
- `rules/code-style.md` - **完整编码规范** ⭐
- `CLAUDE.md` - Claude Code/Ducc 项目指引
- `ARCHITECTURE.md` - 详细架构说明

---

## 快速规范参考

### 代码风格要点

**TypeScript/React**:
- 函数组件 + Hooks
- 避免 `any`，使用明确类型
- 文件命名: 组件 PascalCase，工具 camelCase
- 导入顺序: React → 第三方库 → 项目模块 → 样式

**Java/Spring Boot**:
- 分层架构: Controller → Service → Repository
- RESTful API 设计
- 统一响应格式

**注释规范**:
⚠️ **所有注释必须使用中文** (参考 `rules/code-style.md`)

### Git 提交规范

格式: `[类型]简短描述`

类型标签:
```
[Feature]  - 新功能
[FIXBUG]   - Bug 修复
[Refactor] - 重构
[Docs]     - 文档更新
[Style]    - 样式/格式调整
[Chore]    - 构建/工具/配置
[LOG]      - 更新日志
```

示例:
```bash
git commit -m "[Feature]添加流量统计页面"
git commit -m "[FIXBUG]修复预览同步问题"
```

⚠️ **重要**: 代码修改时必须同步更新 `CHANGELOG.md` (详见 `rules/code-style.md`)

---

## 文件组织

新增功能时的文件位置:
```
frontend/src/admin/pages/XxxPage.tsx        # 前端页面
frontend/src/admin/services/xxx.ts          # API 服务
backend/.../controller/XxxController.java   # 后端控制器
backend/.../service/XxxService.java         # 业务逻辑
backend/.../dto/response/XxxDTO.java        # 数据对象
```

Admin 路由配置位置: `frontend/src/admin/App.tsx` 中的 `items` 数组和 `renderContent` 函数

---

## 错误处理

**前端**: try-catch + Toast/Message 提示 + 控制台日志
**后端**: 自定义异常 + 全局异常处理器 + 日志记录

---

## 性能优化

- React: `useMemo`/`useCallback`、代码分割、虚拟滚动
- 数据库: 索引、分页、避免 N+1 查询
- 打包: Tree-shaking、按需加载、资源压缩

---

## 安全注意事项

- API Key 使用环境变量，不提交到 Git
- 用户输入进行转义，防止 XSS
- 使用参数化查询，防止 SQL 注入
- Spring Security + JWT 认证

---

## 常用命令

```bash
# 前端开发
cd frontend
npm install
npm run dev              # 启动开发服务器
npm run build            # 生产构建
npm run electron:dev     # Electron 开发模式

# 后端开发
cd backend
mvn spring-boot:run      # 启动服务
mvn clean package        # 打包

# Docker
docker-compose up -d               # 启动
docker-compose down                # 停止
docker-compose logs -f [service]   # 查看日志
```

---

## Cursor AI 特定配置

### 代码补全优先级
1. 遵循项目现有代码模式
2. 参考 `rules/code-style.md` 中的规范
3. 保持与周围代码风格一致

### 重构建议
- 优先建议符合项目架构的重构方案
- 保持向后兼容性
- 考虑性能影响

### 注释生成
- 自动生成中文注释
- 包含参数和返回值说明
- 解释复杂逻辑和边界条件

---

## 最佳实践

### 组件设计
```typescript
// ✅ 好的做法
function Editor() {
  return (
    <div>
      <Toolbar />
      <CodeEditor />
      <StatusBar />
    </div>
  );
}

// ❌ 不好的做法 - 单个组件过于复杂
function MegaComponent() {
  // 100+ 行代码...
}
```

### API 调用
```typescript
// ✅ 好的做法
try {
  const result = await apiCall();
  toast.success('操作成功');
  return result;
} catch (error) {
  toast.error(error.message || '操作失败');
  console.error('Error:', error);
}
```

### Java 控制器
```java
// ✅ 好的做法
@RestController
@RequestMapping("/api/admin/xxx")
@RequiredArgsConstructor
public class XxxController {
    private final XxxService xxxService;

    @GetMapping
    public ResponseEntity<?> getList() {
        // 简洁的控制器逻辑
        return ResponseEntity.ok(xxxService.getList());
    }
}
```

---

## 测试指南

- 新功能添加测试用例
- 修改功能确保现有测试通过
- API 修改更新文档

---

## 故障排查

### Monaco Editor 问题
- 加载慢: 使用 CDN + 按需加载
- 大文件卡顿: 限制文件大小或虚拟滚动

### AI 服务问题
- API 调用失败: 检查 API Key、网络、配额

### Electron 打包问题
- 清理并重装依赖: `rm -rf node_modules && npm install`
- 检查 Node.js 版本兼容性

---

## 参考资源

- [React 文档](https://react.dev/)
- [TypeScript 手册](https://www.typescriptlang.org/docs/)
- [Spring Boot 指南](https://spring.io/guides)
- [Tailwind CSS](https://tailwindcss.com/)
- [Ant Design](https://ant.design/)

---

💡 **提示**:
- 完整的编码规范请查看 `rules/code-style.md`
- 项目架构详情请查看 `ARCHITECTURE.md`
- AI 助手使用指引请查看 `CLAUDE.md`
