---
name: jsonutils-maintainer
description: JSONUtils 项目维护技能。用于优化性能、重构可维护性较差的模块、补充 Scheme/CMD 解析能力、维护前后端测试门禁、更新 AI 协作规范和部署排查流程。
---

# JSONUtils Claude Skill Adapter

这是 Claude Code 的项目级派生发现入口，不是独立 skill 源码。

执行前必须完整读取 `../../../.agents/skills/jsonutils-maintainer/SKILL.md`，并把该文件作为唯一权威语义；如无法读取，必须停止使用本适配器并明确报告，不得用本文件替代。

此静态适配器只证明项目发现入口和映射契约存在，不证明真实 skill 选择、加载或行为通过。
